meta {
  name: CSV - Upload File
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/csv/file
  body: multipartForm
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:multipart-form {
  file: @{{csvFilePath}}
}
