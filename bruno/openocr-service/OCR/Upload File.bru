meta {
  name: OCR - Upload File
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/ocr
  body: multipartForm
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:multipart-form {
  file: @{{ocrFilePath}}
}
